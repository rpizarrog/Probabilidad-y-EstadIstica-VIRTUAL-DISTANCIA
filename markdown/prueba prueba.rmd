---
title: "prueba prueba"
author: "Rubén Pizarro Gurrola"
date: "17/11/2021"
output: 
  html_document:
    code_folding: hide
    toc: true
    toc_float: true
    toc_depth: 6
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

# Including Plots

You can also embed plots, for example:

```{r pressure}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

# Interpretación

Existe una relación entre la variable temperatara la variable presión del conjunto de datos que se relaciona con motores de carros.

Con respecto a los valores estadísticos, el valor de la media de la variable velocidad es 15.4 m/h; con respecto a la variable distancia, el valor medio es 48 Metros o Yardas o millas ???.
